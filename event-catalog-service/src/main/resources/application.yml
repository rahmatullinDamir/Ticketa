spring:
  application:
    name: event-catalog-service
  data:
    mongodb:
      uri: ${MONGO_URI}
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"